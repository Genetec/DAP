<UserControl
    x:Class="Genetec.Dap.CodeSamples.Client.CustomConfigPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="0"
            Margin="4"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Padding="0"
            Content="Port"
            Style="{Styles {x:Static LabelStyles.Intro}}" />
        <NumericUpDown
            Grid.Row="0"
            Grid.Column="1"
            MinWidth="75"
            Margin="4"
            HorizontalAlignment="Left"
            Maximum="65535"
            Minimum="1"
            NumberOfDecimalDigits="0"
            Value="{Binding Port, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

        <Label
            Grid.Row="1"
            Grid.Column="1"
            Margin="4,0,4,4"
            Padding="0"
            HorizontalContentAlignment="Stretch"
            Style="{Styles {x:Static LabelStyles.Normal}}">
            <TextBlock
                TextWrapping="Wrap"
                Text="The server accepts HTTP requests only from this computer at 127.0.0.1." />
        </Label>
    </Grid>
</UserControl>
